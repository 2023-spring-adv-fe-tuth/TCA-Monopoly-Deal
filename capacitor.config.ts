import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TCA-Monopoly-Deal',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
